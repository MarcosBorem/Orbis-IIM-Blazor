@inherits LayoutComponentBase

<MudThemeProvider @bind-IsDarkMode="_isDarkMode" Theme="Configuration.Theme" /> @*base em temas*@
<MudSnackbarProvider />

<MudLayout>
    <MudSwitch @bind-Value="@_isDarkMode" />
    <MudMainContent>
        <MudContainer>
            @*largura máxima*@
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>
@code {
    private bool _isDarkMode = true;
}